## NextJS + Tauri + SQLite + Drizzle with migrations

Install dependencies:

```bash
npm install
```

Run migrations:

```bash
npm run migrate
```

Start the app:

```bash
npm run tauri dev
```

Build the app:

```bash
npm run tauri build
```

